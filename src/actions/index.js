// import * as actions from './actions';
import {bestsellersRequested,
        bestsellersLoaded,
        bestsellersError,
        coffeeRequested,
        coffeeLoaded,
        coffeeError,
        goodsRequested,
        goodsLoaded,
        goodsError} from './actions';

export {
  bestsellersRequested,
  bestsellersLoaded,
  bestsellersError,
  coffeeRequested,
  coffeeLoaded,
  coffeeError,
  goodsRequested,
  goodsLoaded,
  goodsError
}
